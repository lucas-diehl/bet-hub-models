# Copy to ".env" (gitignored) and fill in. bootstrap.R loads these automatically.
# DataGolf is the only paid feed (golf only) — you already subscribe. Everything
# else (DK salaries, WNBA/tennis stats, ownership) is free.
DATAGOLF_API_KEY=
