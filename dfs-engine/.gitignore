# secrets
.env
.Renviron
config/secrets.yml
config/dk_session.txt
config/contest_watchlist.txt
*.key

# local store + data (don't commit; but NEVER delete data/ownership — un-backfillable)
data/dfs.duckdb
data/raw/
data/slates/
data/lineups/
data/reports/
data/ownership_inbox/
# data/models/ — INTENTIONALLY tracked in the monorepo: the GitHub Action runs the
# committed models (it has no access to your laptop), and publish_models.ps1 pushes
# retrained weights here. Do NOT re-ignore this.

# R
.Rhistory
.RData
.Rproj.user/
tests/parity/_*.json
